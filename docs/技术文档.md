> 本项目旨在设计并实现一个基于Spring Boot架构、集成AI推荐功能的个性化博客平台，<font style="color:rgb(17, 17, 51);">旨在为用户提供一个简洁、高效、可定制的内容创作与展示空间。博客支持文章发布、分类管理、标签检索、评论互动等核心功能，并注重响应式设计与用户体验，并</font>通过AI推荐技术实现个性化内容分发，提升用户获取有价值内容的效率，适配web端和移动端访问。
>
> 目标时间：3-4个月
>

[项目计划](https://www.yuque.com/jasonwang-fiek0/gv40l9/gm4givecxeb15qog)

# 一、技术栈
后端：SpringBoot + MySQL + Redis + Swagger  (RESTful API + JWT认证）

Web前端：Vue3 + Ts + Vite + Pinia + Axios +（Element Plus/<font style="color:rgb(17, 17, 51);">Ant Design</font>）

移动端APP：<font style="color:rgb(17, 17, 51);">React Native + TS</font>（Android+IOS）

# 二、开发工具
+ IntelliJ IDEA Ultimate
+ VS Code 
+ <font style="color:#000000;">Android Studio (Flutter开发)</font>
+ Navicat(数据库)
+ Postman(接口测试)
+ Git
+ Chrome + Vue DevTools  
+ Figma/Axure (原型设计)
+ 语雀(流程图/ER图)
+ 语雀(文档管理)
+ GitHub (代码托管)

# 三、核心功能
[核心功能](https://www.yuque.com/jasonwang-fiek0/gv40l9/aus7pbwpazpashxr)

# 四、开发环境搭建
## Github分支策略
### 主分支（长期存在）
+ **main**

:::info
作用: 生产环境代码,永远保持稳定可部署状态  
保护规则: 

+ 只能通过 PR 合并
+ 需要 Code Review
+ 所有测试必须通过

:::

+ **develop**

:::info
作用: 开发主分支,日常开发的集成分支  
保护规则:

+ 通过 PR 合并
+ 功能开发完成后合并到此

:::

### 辅助分支 (临时分支,用完即删)  
+ **feature**

:::info
作用: 开发新功能 

从哪来: develop 

合并到: develop 

命名规范: feature/功能名称 

生命周期: 功能开发完成后删除

:::

+ **release**

:::info
作用: 准备新版本发布,只做 bug 修复和版本号更新 

从哪来: develop 

合并到: main 和 develop 

命名规范: release/版本号 生命周期: 发布完成后删除

:::

+  **hotfix**

:::info
作用: 紧急修复生产环境 bug 

从哪来: main 

合并到: main 和 develop 

命名规范: hotfix/bug描述 

生命周期: 修复完成后删除

:::

## 项目结构
### 后端
```plain
Wintina
│  CHANGELOG.md
│  README.md
│
├─backend
│  │  .gitattributes
│  │  .gitignore
│  │  HELP.md
│  │  mvnw
│  │  mvnw.cmd
│  │  pom.xml
│  │
│  ├─.idea
│  │  │  .gitignore
│  │  │  misc.xml
│  │  │  workspace.xml
│  │  │
│  │  └─inspectionProfiles
│  │          Project_Default.xml
│  │
│  ├─.mvn
│  │  └─wrapper
│  │          maven-wrapper.properties
│  │
│  └─src
│      ├─main
│      │  ├─java
│      │  │  └─com
│      │  │      └─wintina
│      │  │          └─blog
│      │  │              │  BackendApplication.java
│      │  │              │
│      │  │              ├─common
│      │  │              ├─config
│      │  │              ├─controller
│      │  │              ├─entity
│      │  │              └─service
│      │  │                  │  package-info.java
│      │  │                  │
│      │  │                  └─impl
│      │  └─resources
│      │      │  application.properties
│      │      │
│      │      ├─static
│      │      └─templates
│      └─test
│          └─java
│              └─com
│                  └─wintina
│                      └─blog
│                              BackendApplicationTests.java
│
├─docker
├─docs
├─mobile
├─scripts
└─web
```

### 前端
```plain
web/
├── public/
│   ├── favicon.ico
│   └── logo.png
│
├── src/
│   ├── api/                          # API 接口
│   │   ├── index.ts                 # API 统一导出
│   │   ├── request.ts               # Axios 封装
│   │   ├── auth.ts                  # 认证相关接口
│   │   ├── user.ts                  # 用户相关接口
│   │   ├── post.ts                  # 文章相关接口
│   │   ├── comment.ts               # 评论相关接口
│   │   ├── category.ts              # 分类相关接口
│   │   ├── tag.ts                   # 标签相关接口
│   │   └── admin/                   # 后台管理接口
│   │       ├── dashboard.ts         # 仪表盘
│   │       ├── post.ts              # 文章管理
│   │       ├── comment.ts           # 评论管理
│   │       └── user.ts              # 用户管理
│   │
│   ├── assets/                       # 静态资源
│   │   ├── images/                  # 图片
│   │   │   ├── logo.png
│   │   │   ├── placeholder.png
│   │   │   └── default-avatar.png
│   │   ├── styles/                  # 样式
│   │   │   ├── index.scss           # 全局样式
│   │   │   ├── variables.scss       # 变量
│   │   │   ├── blog.scss            # 博客前台样式
│   │   │   └── admin.scss           # 后台样式
│   │   └── icons/                   # 图标(SVG)
│   │
│   ├── components/                   # 公共组件
│   │   ├── common/                  # 通用组件
│   │   │   ├── Loading.vue
│   │   │   ├── Empty.vue
│   │   │   ├── Pagination.vue
│   │   │   └── ConfirmDialog.vue
│   │   ├── blog/                    # 博客前台组件
│   │   │   ├── Header.vue
│   │   │   ├── Footer.vue
│   │   │   ├── Sidebar.vue
│   │   │   ├── ArticleCard.vue
│   │   │   ├── CommentList.vue
│   │   │   ├── CommentItem.vue
│   │   │   └── MarkdownViewer.vue
│   │   └── admin/                   # 后台专用组件
│   │       ├── AdminHeader.vue
│   │       ├── AdminSidebar.vue
│   │       ├── DataCard.vue
│   │       └── MarkdownEditor.vue
│   │
│   ├── layouts/                      # 布局组件
│   │   ├── BlogLayout.vue           # 博客前台布局
│   │   ├── AdminLayout.vue          # 管理后台布局
│   │   └── EmptyLayout.vue          # 空布局(登录页用)
│   │
│   ├── views/                        # 页面组件
│   │   ├── blog/                    # 【博客前台页面】
│   │   │   ├── Home.vue            # 首页
│   │   │   ├── PostList.vue        # 文章列表
│   │   │   ├── PostDetail.vue      # 文章详情
│   │   │   ├── Category.vue        # 分类页
│   │   │   ├── Tag.vue             # 标签页
│   │   │   ├── Archive.vue         # 归档页
│   │   │   ├── About.vue           # 关于页
│   │   │   ├── Message.vue         # 留言板
│   │   │   ├── Gallery.vue         # 图库
│   │   │   ├── Resource.vue        # 资源/工具
│   │   │   ├── HeartIsle.vue       # Heart Isle
│   │   │   └── Search.vue          # 搜索页
│   │   │
│   │   ├── auth/                    # 【认证页面】
│   │   │   ├── Login.vue           # 登录
│   │   │   └── Register.vue        # 注册
│   │   │
│   │   ├── user/                    # 【用户中心】
│   │   │   ├── Profile.vue         # 个人资料
│   │   │   ├── MyPosts.vue         # 我的文章
│   │   │   └── Settings.vue        # 设置
│   │   │
│   │   └── admin/                   # 【管理后台页面】
│   │       ├── Dashboard.vue        # 仪表盘
│   │       ├── post/                # 文章管理
│   │       │   ├── List.vue        # 文章列表
│   │       │   ├── Edit.vue        # 编辑文章
│   │       │   └── Create.vue      # 新建文章
│   │       ├── comment/             # 评论管理
│   │       │   └── List.vue
│   │       ├── category/            # 分类管理
│   │       │   └── List.vue
│   │       ├── tag/                 # 标签管理
│   │       │   └── List.vue
│   │       ├── gallery/             # 图库管理
│   │       │   └── List.vue
│   │       ├── resource/            # 资源管理
│   │       │   └── List.vue
│   │       ├── user/                # 用户管理
│   │       │   └── List.vue
│   │       ├── message/             # 留言管理
│   │       │   └── List.vue
│   │       └── system/              # 系统管理
│   │           ├── Settings.vue
│   │           └── Logs.vue
│   │
│   ├── router/                       # 路由配置
│   │   ├── index.ts                 # 主路由
│   │   ├── blog.ts                  # 博客路由
│   │   └── admin.ts                 # 后台路由
│   │
│   ├── stores/                       # Pinia 状态管理
│   │   ├── index.ts                 # Store 统一导出
│   │   ├── user.ts                  # 用户状态
│   │   ├── app.ts                   # 应用状态
│   │   └── admin.ts                 # 后台状态
│   │
│   ├── utils/                        # 工具函数
│   │   ├── auth.ts                  # 认证工具
│   │   ├── storage.ts               # 本地存储
│   │   ├── format.ts                # 格式化工具
│   │   ├── validate.ts              # 验证工具
│   │   └── permission.ts            # 权限判断
│   │
│   ├── types/                        # TypeScript 类型定义
│   │   ├── index.ts                 # 类型统一导出
│   │   ├── user.ts                  # 用户类型
│   │   ├── post.ts                  # 文章类型
│   │   ├── comment.ts               # 评论类型
│   │   ├── api.ts                   # API 响应类型
│   │   └── common.ts                # 公共类型
│   │
│   ├── directives/                   # 自定义指令(可选)
│   │   └── permission.ts            # 权限指令
│   │
│   ├── App.vue                       # 根组件
│   └── main.ts                       # 入口文件
│
├── .env.development                  # 开发环境变量
├── .env.production                   # 生产环境变量
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## 版本
+ springboot 3.5.8
+ JDK21
+ Java语言级别21
+ Maven 4.0.0
+ MySQL 8.0

# 五、开发规范
## Java代码规范
参考阿里巴巴Java代码规范：

[Java开发手册（嵩山版）.pdf](https://www.yuque.com/attachments/yuque/0/2025/pdf/63570694/1765964262226-b0a76e12-275d-41dc-a2cc-dbf3b81944d1.pdf)

## 前端开发规范
参考阿里巴巴前端开发规范：

[前端开发规范-阿里云开发者社区](https://developer.aliyun.com/article/1249471)

## Redis开发规范及常见问题
[Redis 的开发规范和常见问题 | 学习笔记-阿里云开发者社区](https://developer.aliyun.com/article/1082681?spm=5176.21213303.J_ZGek9Blx07Hclc3Ddt9dg.15.d4ef2f3dByCOkl&scm=20140722.S_community@@%E6%96%87%E7%AB%A0@@1082681._.ID_community@@%E6%96%87%E7%AB%A0@@1082681-RL_%E5%BC%80%E5%8F%91%E8%A7%84%E8%8C%83-LOC_2024SPAllResult-OR_ser-PAR1_213e030a17662461215151435e24b6-V_4-PAR3_o-RE_new5-P0_5-P1_0)

## API设计规范
[api设计规范-阿里云开发者社区](https://developer.aliyun.com/article/709322?spm=5176.21213303.J_ZGek9Blx07Hclc3Ddt9dg.201.d4ef2f3dByCOkl&scm=20140722.S_community@@%E6%96%87%E7%AB%A0@@709322._.ID_community@@%E6%96%87%E7%AB%A0@@709322-RL_API%E8%A7%84%E8%8C%83-LOC_2024SPAllResult-OR_ser-PAR1_213e030a17662463825657642e24b6-V_4-PAR3_o-RE_new5-P0_5-P1_0)

# 六、数据库设计
## E-R图
<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/63570694/1766807677207-d8286c02-5782-40ab-bf08-7e439de6e331.png)

## SQL
```sql
-- ==============================================
-- 第一步：创建无外键依赖的基础表（核心基础配置）
-- ==============================================
-- 1. 角色表（无依赖）
CREATE TABLE `role` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '角色ID',
  `role_name` VARCHAR(50) NOT NULL UNIQUE COMMENT '角色名称',
  `role_label` VARCHAR(50) NOT NULL UNIQUE COMMENT '角色标识',
  `description` VARCHAR(255) COMMENT '角色描述',
  `status` TINYINT DEFAULT 1 COMMENT '状态(0:禁用 1:正常)',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_is_deleted` (`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色表';

-- 2. 系统配置表（无依赖）
CREATE TABLE `config` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '配置ID',
  `config_key` VARCHAR(100) NOT NULL UNIQUE COMMENT '配置键',
  `config_value` TEXT COMMENT '配置值',
  `description` VARCHAR(500) COMMENT '配置描述',
  `type` VARCHAR(20) DEFAULT 'STRING' COMMENT '类型: STRING, NUMBER, BOOLEAN, JSON',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_is_deleted` (`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='系统配置表';

-- 3. 访问统计表（无依赖）
CREATE TABLE `visit_stats` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
  `date` DATE NOT NULL COMMENT '日期',
  `pv` INT DEFAULT 0 COMMENT '页面浏览量',
  `uv` INT DEFAULT 0 COMMENT '独立访客数',
  `ip_count` INT DEFAULT 0 COMMENT 'IP数量',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  UNIQUE KEY `uk_date` (`date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='访问统计表';

-- ==============================================
-- 第二步：创建依赖基础表的核心表
-- ==============================================
-- 4. 用户表（依赖role表）
CREATE TABLE `user` (
  `user_id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
  `username` VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
  `password` VARCHAR(255) NOT NULL COMMENT '密码(BCrypt加密)',
  `nickname` VARCHAR(50) COMMENT '昵称',
  `email` VARCHAR(100) UNIQUE COMMENT '邮箱',
  `phone` VARCHAR(20) COMMENT '手机号',
  `avatar` VARCHAR(255) COMMENT '头像URL',
  `intro` VARCHAR(500) COMMENT '个人简介',
  `website` VARCHAR(255) COMMENT '个人网站',
  `role_id` BIGINT COMMENT '角色ID',
  `login_type` TINYINT COMMENT '登录方式(1:账号 2:微信 3:QQ)',
  `ip_address` VARCHAR(50) COMMENT '登录IP',
  `ip_source` VARCHAR(100) COMMENT 'IP来源',
  `last_login_time` DATETIME COMMENT '最后登录时间',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_username` (`username`),
  INDEX `idx_email` (`email`),
  FOREIGN KEY (`role_id`) REFERENCES `role`(`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';

-- 5. 菜单/资源表（自关联，无外部依赖）
CREATE TABLE `menu` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '菜单ID',
  `parent_id` BIGINT DEFAULT 0 COMMENT '父菜单ID',
  `menu_name` VARCHAR(50) NOT NULL COMMENT '菜单名称',
  `path` VARCHAR(255) COMMENT '路由路径',
  `component` VARCHAR(255) COMMENT '组件路径',
  `icon` VARCHAR(50) COMMENT '菜单图标',
  `order_num` INT DEFAULT 0 COMMENT '排序',
  `menu_type` TINYINT COMMENT '类型(1:目录 2:菜单 3:按钮)',
  `permission` VARCHAR(100) COMMENT '权限标识',
  `is_hidden` TINYINT DEFAULT 0 COMMENT '是否隐藏(0:显示 1:隐藏)',
  `status` TINYINT DEFAULT 1 COMMENT '状态(0:禁用 1:正常)',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_parent_id` (`parent_id`),
  INDEX `idx_menu_type` (`menu_type`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`parent_id`) REFERENCES `menu`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='菜单资源表';

-- 6. 博文分类表（自关联，无外部依赖）
CREATE TABLE `category` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '分类ID',
  `name` VARCHAR(50) NOT NULL UNIQUE COMMENT '分类名称',
  `slug` VARCHAR(50) NOT NULL UNIQUE COMMENT '分类别名',
  `description` VARCHAR(255) COMMENT '分类描述',
  `parent_id` BIGINT DEFAULT 0 COMMENT '父分类ID',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `status` TINYINT DEFAULT 1 COMMENT '状态(0:禁用 1:正常)',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_parent_id` (`parent_id`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`parent_id`) REFERENCES `category`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='博文分类表';

-- ==============================================
-- 第三步：创建关联表（依赖多个基础表）
-- ==============================================
-- 7. 角色-菜单关联表（依赖role、menu表）
CREATE TABLE `role_menu` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
  `role_id` BIGINT NOT NULL COMMENT '角色ID',
  `menu_id` BIGINT NOT NULL COMMENT '菜单ID',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  UNIQUE KEY `uk_role_menu` (`role_id`, `menu_id`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`role_id`) REFERENCES `role`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`menu_id`) REFERENCES `menu`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='角色菜单关联表';

-- ==============================================
-- 第四步：创建核心业务表（依赖user、category等）
-- ==============================================
-- 8. 博文表（依赖user、category表）
CREATE TABLE `post` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '文章ID',
  `user_id` BIGINT NOT NULL COMMENT '作者ID',
  `category_id` BIGINT COMMENT '分类ID',
  `title` VARCHAR(200) NOT NULL COMMENT '文章标题',
  `slug` VARCHAR(200) NOT NULL UNIQUE COMMENT '文章别名(URL)',
  `summary` TEXT COMMENT '文章摘要',
  `content` LONGTEXT NOT NULL COMMENT '文章内容(Markdown)',
  `content_html` LONGTEXT COMMENT '文章内容(HTML)',
  `cover_image` VARCHAR(255) COMMENT '封面图片',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-草稿, 1-已发布, 2-私密',
  `is_top` TINYINT DEFAULT 0 COMMENT '是否置顶: 0-否, 1-是',
  `is_recommend` TINYINT DEFAULT 0 COMMENT '是否推荐: 0-否, 1-是',
  `allow_comment` TINYINT DEFAULT 1 COMMENT '是否允许评论: 0-否, 1-是',
  `view_count` INT DEFAULT 0 COMMENT '浏览次数',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `comment_count` INT DEFAULT 0 COMMENT '评论数',
  `collect_count` INT DEFAULT 0 COMMENT '收藏数',
  `sort_order` INT DEFAULT 0 COMMENT '排序',
  `seo_keywords` VARCHAR(200) COMMENT 'SEO关键词',
  `seo_description` VARCHAR(500) COMMENT 'SEO描述',
  `publish_time` DATETIME COMMENT '发布时间',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_category_id` (`category_id`),
  INDEX `idx_status` (`status`),
  INDEX `idx_publish_time` (`publish_time`),
  INDEX `idx_is_top_recommend` (`is_top`, `is_recommend`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FULLTEXT INDEX `idx_title_content` (`title`, `content`) WITH PARSER ngram,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`user_id`) ON DELETE CASCADE,
  FOREIGN KEY (`category_id`) REFERENCES `category`(`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='博文表';

-- 9. 博文标签表（无外部依赖）
CREATE TABLE `tag` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '标签ID',
  `name` VARCHAR(50) NOT NULL UNIQUE COMMENT '标签名称',
  `slug` VARCHAR(50) NOT NULL UNIQUE COMMENT '标签别名',
  `color` VARCHAR(20) COMMENT '标签颜色',
  `post_count` INT DEFAULT 0 COMMENT '文章数量',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_name` (`name`),
  INDEX `idx_is_deleted` (`is_deleted`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='标签表';

-- 10. 博文-标签关联表（依赖post、tag表）
CREATE TABLE `post_tag` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
  `post_id` BIGINT NOT NULL COMMENT '文章ID',
  `tag_id` BIGINT NOT NULL COMMENT '标签ID',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  UNIQUE KEY `uk_post_tag` (`post_id`, `tag_id`),
  INDEX `idx_post_id` (`post_id`),
  INDEX `idx_tag_id` (`tag_id`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`post_id`) REFERENCES `post`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`tag_id`) REFERENCES `tag`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='博文-标签关联表';

-- ==============================================
-- 第五步：创建互动/资源类表（依赖user、post等）
-- ==============================================
-- 11. 评论表（依赖user、post表）
CREATE TABLE `comment` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '评论ID',
  `post_id` BIGINT NOT NULL COMMENT '文章ID',
  `user_id` BIGINT COMMENT '用户ID(游客为NULL)',
  `parent_id` BIGINT DEFAULT 0 COMMENT '父评论ID, 0表示顶级评论',
  `reply_to_id` BIGINT COMMENT '回复的评论ID',
  `content` TEXT NOT NULL COMMENT '评论内容',
  `nickname` VARCHAR(50) COMMENT '昵称(游客)',
  `email` VARCHAR(100) COMMENT '邮箱(游客)',
  `website` VARCHAR(200) COMMENT '网站(游客)',
  `ip` VARCHAR(50) COMMENT 'IP地址',
  `user_agent` VARCHAR(500) COMMENT '用户代理',
  `like_count` INT DEFAULT 0 COMMENT '点赞数',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-待审核, 1-已通过, 2-已拒绝',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_post_id` (`post_id`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_parent_id` (`parent_id`),
  INDEX `idx_status` (`status`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`post_id`) REFERENCES `post`(`id`) ON DELETE CASCADE,
  FOREIGN KEY (`user_id`) REFERENCES `user`(`user_id`) ON DELETE SET NULL,
  FOREIGN KEY (`reply_to_id`) REFERENCES `comment`(`id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='评论表';

-- 12. 留言板表（依赖user表）
CREATE TABLE `message` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '留言ID',
  `user_id` BIGINT COMMENT '用户ID',
  `parent_id` BIGINT DEFAULT 0 COMMENT '父留言ID',
  `content` TEXT NOT NULL COMMENT '留言内容',
  `nickname` VARCHAR(50) COMMENT '昵称(游客)',
  `email` VARCHAR(100) COMMENT '邮箱(游客)',
  `website` VARCHAR(200) COMMENT '网站(游客)',
  `ip` VARCHAR(50) COMMENT 'IP地址',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-待审核, 1-已通过, 2-已拒绝',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_status` (`status`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`user_id`) REFERENCES `user`(`user_id`) ON DELETE SET NULL,
  FOREIGN KEY (`parent_id`) REFERENCES `message`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='留言板表';

-- 13. 图库表（依赖user表）
CREATE TABLE `image` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '图片ID',
  `user_id` BIGINT NOT NULL COMMENT '上传用户ID',
  `name` VARCHAR(200) NOT NULL COMMENT '图片名称',
  `url` VARCHAR(500) NOT NULL COMMENT '图片URL',
  `thumbnail_url` VARCHAR(500) COMMENT '缩略图URL',
  `size` BIGINT COMMENT '文件大小(字节)',
  `width` INT COMMENT '图片宽度',
  `height` INT COMMENT '图片高度',
  `mime_type` VARCHAR(50) COMMENT 'MIME类型',
  `storage_type` VARCHAR(20) DEFAULT 'LOCAL' COMMENT '存储类型: LOCAL, OSS, COS',
  `storage_path` VARCHAR(500) COMMENT '存储路径',
  `description` VARCHAR(500) COMMENT '图片描述',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`user_id`) REFERENCES `user`(`user_id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='图库表';

-- 14. 资源/工具表（依赖user表）
CREATE TABLE `resource` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '资源ID',
  `user_id` BIGINT NOT NULL COMMENT '发布用户ID',
  `title` VARCHAR(200) NOT NULL COMMENT '资源标题',
  `description` TEXT COMMENT '资源描述',
  `url` VARCHAR(500) NOT NULL COMMENT '资源链接',
  `file_url` VARCHAR(500) COMMENT '文件下载地址',
  `file_size` BIGINT COMMENT '文件大小',
  `file_type` VARCHAR(50) COMMENT '文件类型',
  `icon` VARCHAR(255) COMMENT '图标',
  `type` VARCHAR(20) NOT NULL COMMENT '类型: TOOL-工具, RESOURCE-资源',
  `download_count` INT DEFAULT 0 COMMENT '下载次数',
  `view_count` INT DEFAULT 0 COMMENT '查看次数',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-下架, 1-上架',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_type` (`type`),
  INDEX `idx_status` (`status`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`user_id`) REFERENCES `user`(`user_id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='资源/工具表';

-- ==============================================
-- 第六步：创建用户行为表（依赖user、post等）
-- ==============================================
-- 15. 用户点赞表（依赖user表）
CREATE TABLE `user_like` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `target_type` VARCHAR(20) NOT NULL COMMENT '目标类型: POST, COMMENT',
  `target_id` BIGINT NOT NULL COMMENT '目标ID',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  UNIQUE KEY `uk_user_like` (`user_id`, `target_type`, `target_id`),
  INDEX `idx_target` (`target_type`, `target_id`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`user_id`) REFERENCES `user`(`user_id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户点赞表';

-- 16. 用户收藏表（依赖user、post表）
CREATE TABLE `user_collect` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT 'ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `post_id` BIGINT NOT NULL COMMENT '文章ID',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  UNIQUE KEY `uk_user_collect` (`user_id`, `post_id`),
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_post_id` (`post_id`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`user_id`) REFERENCES `user`(`user_id`) ON DELETE CASCADE,
  FOREIGN KEY (`post_id`) REFERENCES `post`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户收藏表';

-- ==============================================
-- 第七步：创建日志类表（依赖user表）
-- ==============================================
-- 17. 操作日志表（依赖user表）
CREATE TABLE `operation_log` (
  `id` BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '日志ID',
  `user_id` BIGINT COMMENT '用户ID',
  `username` VARCHAR(50) COMMENT '用户名',
  `operation` VARCHAR(100) NOT NULL COMMENT '操作内容',
  `method` VARCHAR(200) COMMENT '请求方法',
  `params` TEXT COMMENT '请求参数',
  `result` TEXT COMMENT '返回结果',
  `ip` VARCHAR(50) COMMENT 'IP地址',
  `location` VARCHAR(100) COMMENT '操作地点',
  `browser` VARCHAR(100) COMMENT '浏览器',
  `os` VARCHAR(100) COMMENT '操作系统',
  `status` TINYINT DEFAULT 1 COMMENT '状态: 0-失败, 1-成功',
  `error_msg` TEXT COMMENT '错误信息',
  `cost_time` INT COMMENT '耗时(毫秒)',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `is_deleted` TINYINT DEFAULT 0 COMMENT '逻辑删除(0:未删除 1:已删除)',
  INDEX `idx_user_id` (`user_id`),
  INDEX `idx_create_time` (`create_time`),
  INDEX `idx_status` (`status`),
  INDEX `idx_is_deleted` (`is_deleted`),
  FOREIGN KEY (`user_id`) REFERENCES `user`(`user_id`) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='操作日志表';
```

# 七、API接口文档


